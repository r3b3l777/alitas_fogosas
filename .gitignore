node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Variables locales (llaves de Supabase) — nunca al repo
.env
.env.local
.env.*.local

# Reportes de auditoría generados por herramientas — no son parte del sitio
motion-audits/
